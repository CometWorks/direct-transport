<?xml version="1.0"?>
<PluginData xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:type="GitHubPlugin">

    <Id>direct-transport</Id>
    <GroupId>direct-transport</GroupId>
    <RepoId>CometWorks/direct-transport</RepoId>

    <FriendlyName>Direct Transport</FriendlyName>
    <Author>CometWorks</Author>

    <Tooltip>Non-Steam direct UDP multiplayer transport (client)</Tooltip>
    <Description>Lets a Space Engineers client join a dedicated server over raw UDP
        without Steam, for headless multi-client test setups (functional and load
        testing). Activated by the --connect option on the command line. Also
        provides --client-id (fake Steam identity, own user data folder,
        concurrent instances) and --client-name (player name).
        Requires the matching server-side Direct Transport plugin on the server.
    </Description>

    <SourceDirectories>
        <Directory>ClientPlugin</Directory>
        <Directory>Shared</Directory>
    </SourceDirectories>

    <NuGetReferences>
        <PackageReference Include="LiteNetLib" Version="1.3.1" />
    </NuGetReferences>

    <Runtimes>CoreCLR</Runtimes>

    <Hidden>true</Hidden>

    <Commit>TODO</Commit>

</PluginData>
